<?xml version="1.0" encoding="UTF-8"?>
<!--
  menu.ui -- the File/Edit/Format/View/Help GMenu model (menu.asm wraps
  this in a GtkPopoverMenuBar). "_" marks the Alt+X mnemonic, matching
  classic Notepad convention. Items reference actions by name ("win.foo"
  against the GtkApplicationWindow's action map, "app.foo" against the
  GApplication's -- see actions.asm) -- an item pointing at an action that
  doesn't exist yet is automatically rendered insensitive by GTK.

  Each <section> (no visible label) renders as a horizontal separator
  between it and its neighbors -- that's the only way a GMenu model
  produces the classic menu separator line, which is why File and Edit
  below are each split into several sections instead of one flat list.

  The View submenu carries id="view_menu" so format.asm's
  relabel_dark_mode_item can keep fetching this same GMenu* (via
  gtk_builder_get_object in menu.asm) and rewriting the Dark/Light Mode
  item in place at DARK_MODE_MENU_INDEX (consts.inc).
-->
<interface>
  <menu id="menubar">
    <submenu>
      <attribute name="label">_File</attribute>
      <section>
        <item>
          <attribute name="label">_New</attribute>
          <attribute name="action">win.new</attribute>
        </item>
        <item>
          <attribute name="label">_Open...</attribute>
          <attribute name="action">win.open</attribute>
        </item>
        <item>
          <attribute name="label">_Save</attribute>
          <attribute name="action">win.save</attribute>
        </item>
        <item>
          <attribute name="label">Save _As...</attribute>
          <attribute name="action">win.save-as</attribute>
        </item>
      </section>
      <section>
        <item>
          <attribute name="label">Page Set_up...</attribute>
          <attribute name="action">win.page-setup</attribute>
        </item>
        <item>
          <attribute name="label">_Print...</attribute>
          <attribute name="action">win.print</attribute>
        </item>
      </section>
      <section>
        <item>
          <!-- "app." prefix, not "win." -- quitting is application-scoped, registered in actions.asm's app_actions table -->
          <attribute name="label">E_xit</attribute>
          <attribute name="action">app.quit</attribute>
        </item>
      </section>
    </submenu>

    <submenu>
      <attribute name="label">_Edit</attribute>
      <section>
        <item>
          <attribute name="label">_Undo</attribute>
          <attribute name="action">win.undo</attribute>
        </item>
      </section>
      <section>
        <item>
          <attribute name="label">Cu_t</attribute>
          <attribute name="action">win.cut</attribute>
        </item>
        <item>
          <attribute name="label">_Copy</attribute>
          <attribute name="action">win.copy</attribute>
        </item>
        <item>
          <attribute name="label">_Paste</attribute>
          <attribute name="action">win.paste</attribute>
        </item>
        <item>
          <attribute name="label">De_lete</attribute>
          <attribute name="action">win.delete</attribute>
        </item>
      </section>
      <section>
        <item>
          <attribute name="label">_Find...</attribute>
          <attribute name="action">win.find</attribute>
        </item>
        <item>
          <attribute name="label">Find _Next</attribute>
          <attribute name="action">win.find-next</attribute>
        </item>
        <item>
          <attribute name="label">_Replace...</attribute>
          <attribute name="action">win.replace</attribute>
        </item>
        <item>
          <attribute name="label">_Go To...</attribute>
          <attribute name="action">win.go-to-line</attribute>
        </item>
      </section>
      <section>
        <item>
          <attribute name="label">Select _All</attribute>
          <attribute name="action">win.select-all</attribute>
        </item>
        <item>
          <attribute name="label">Time/_Date</attribute>
          <attribute name="action">win.insert-time-date</attribute>
        </item>
      </section>
    </submenu>

    <submenu>
      <attribute name="label">F_ormat</attribute>
      <item>
        <!-- stateful boolean action (see actions.asm) -- GTK renders this as a checkable item automatically -->
        <attribute name="label">_Word Wrap</attribute>
        <attribute name="action">win.word-wrap</attribute>
      </item>
      <item>
        <attribute name="label">_Font...</attribute>
        <attribute name="action">win.font</attribute>
      </item>
    </submenu>

    <submenu id="view_menu">
      <attribute name="label">_View</attribute>
      <item>
        <!-- stateful boolean action -> checkable item -->
        <attribute name="label">_Status Bar</attribute>
        <attribute name="action">win.status-bar</attribute>
      </item>
      <item>
        <!-- initial label text; format.asm's relabel_dark_mode_item swaps this for "Light _Mode" once dark mode is active (a plain stateless action, not a checkbox -- see format.asm) -->
        <attribute name="label">Dark _Mode</attribute>
        <attribute name="action">win.dark-mode</attribute>
      </item>
      <item>
        <!-- also a stateful boolean action -> checkable item; kept AFTER Dark Mode so DARK_MODE_MENU_INDEX (consts.inc) doesn't need to change -->
        <attribute name="label">_Line Numbers</attribute>
        <attribute name="action">win.line-numbers</attribute>
      </item>
    </submenu>

    <submenu>
      <attribute name="label">_Help</attribute>
      <item>
        <attribute name="label">_View Help</attribute>
        <attribute name="action">win.view-help</attribute>
      </item>
      <item>
        <attribute name="label">_About UnbloatedPad</attribute>
        <attribute name="action">win.about</attribute>
      </item>
    </submenu>
  </menu>
</interface>
